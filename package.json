{
  "name": "ciberpty-portfolio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "start": "serve dist -l ${PORT:-3000}"
  },
  "dependencies": {
    "astro": "^5.2.0",
    "serve": "^13.0.4"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
